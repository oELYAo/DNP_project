name: CI
on: [push]
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
      - name: Install pylint
        run: pip install pylint
      - name: Code linting...
        run: |
          pylint_score=$(pylint --exit-zero src/ tests/ | grep -oP 'at \K\d+\.\d+')
          echo "Linter score(out of 10): $pylint_score"
          awk -v score="$pylint_score" 'BEGIN {exit !(score >= 0)}'

  sample_test:
    needs: lint
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run unit tests
      run: |
        pip install pytest
        pytest

    # ------------------------------
    # Hadoop Streaming WordCount
    # ------------------------------
    - name: Set up Java and Hadoop
      run: |
        sudo apt-get update
        sudo apt-get install -y openjdk-11-jdk wget unzip
        wget https://downloads.apache.org/hadoop/common/hadoop-3.3.6/hadoop-3.3.6.tar.gz
        tar -xzf hadoop-3.3.6.tar.gz
        mv hadoop-3.3.6 $HOME/hadoop
        echo "HADOOP_HOME=$HOME/hadoop" >> $GITHUB_ENV
        echo "$HOME/hadoop/bin" >> $GITHUB_PATH
        echo "JAVA_HOME=/usr/lib/jvm/java-11-openjdk-amd64" >> $GITHUB_ENV

    - name: Prepare WordCount test file
      run: |
        echo "foo bar foo" > sample.txt
        $HOME/hadoop/bin/hadoop fs -mkdir -p /tmp/
        $HOME/hadoop/bin/hadoop fs -put sample.txt /tmp/

    - name: Fix line endings in mapper and reducer
      run: |
        sed -i 's/\r$//' src/mapreduce/mapper_wordcount.py
        sed -i 's/\r$//' src/mapreduce/reducer_wordcount.py


    - name: Run Hadoop Streaming WordCount
      run: |
        $HOME/hadoop/bin/hadoop jar $HOME/hadoop/share/hadoop/tools/lib/hadoop-streaming-*.jar \
          -files afinn-111.txt \
          -input /data/cleaned/* \
          -output /data/sentiment_output \
          -mapper mapper_sentiment.py \
          -combiner reducer_sentiment.py \
          -reducer reducer_sentiment.py

    - name: Show WordCount output
      run: |
        $HOME/hadoop/bin/hadoop fs -cat /tmp/sample_out/part-*